var num1 = 30;
        console.log(num1);
        var myNum = 100;
        console.log(myNum);
        var mayur = 40;
        console.log(mayur);

        var totalstudents = 30; //variable withot spaces
        var TotalStudents = 20; //PascalCasing name 
        var totalStudents = 40; // camelCasing name
        var total_student = 70;   // use undersore (_) to the variable

          

        //note : not use any symbol only allow 2 symbols that is ($),(_) only use the symbols.
        // not make variables in like : var 1mark = 30; it is not allwo in write the variables (example: var 123 = 67; , var 12yash = 67 )


        // use like this types is allwo (var  ma23ks = 70; )
         

        var mark2 = 90;
        console.log(mark2);
         

        var king23s = 45;
        console.log(king23s);

